import { FC } from 'react'
import { IProduct } from './types/product.interface'

const DataItem: FC = (data) => {
  return (
    <div>
      {data.map(item => {

      })}
    </div>
  )
}

export default DataItem
